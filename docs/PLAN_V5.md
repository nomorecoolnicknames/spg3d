# PLAN V5 — доводка после 2.3.0 по отзыву владельца

Отзыв владельца (2026-09-15, после выпуска 2.3.0, играл на телефоне/ПК), дословный список проблем:

| # | Проблема (со слов владельца) | Статус |
|---|---|---|
| 1 | окна у домов по-прежнему «дебильно мерцают» | исправлено d21d114 |
| 2 | плеера нет в меню паузы | исправлено a62d3a7 |
| 3 | полёт сквозь стены должен оставаться пасхалкой (не кнопкой в паузе, не в подсказках) | исправлено a62d3a7 |
| 4 | Щёлково слишком не детализировано, должно быть проработанным и **дневным**, не ночным | исправлено 0cf4698 (день, деревья, газоны, ограды, скамейки), 403a74c (отель «Премиум», река), a4dc0b0 (зебры, фонтаны) |
| 5 | освещение «дебильное» | исправлено: день в Щёлково 0cf4698, ночь 1aa5a17 (фары на дороге, пятна фонарей, без неона), двор босса d5387e4 |
| 6 | круги не завершаются после включения полёта | исправлено bb7c2ca |
| 7 | после надписи «Финиш» заставляет проехать ещё один круг | исправлено bb7c2ca |
| 8 | следы шин и свет на трассе дешёвые | исправлено 370d21f (следы, отражения), 0cf4698 (убраны оранжевые пятна-декали) |
| 9 | у BMW пропали передние фары, задние тянут шлейф во время гонки | исправлено 7592d97 (шлейф), 6f4655e (фары) |
| 10 | на лужах нет капель дождя | исправлено 370d21f (круги от капель в лужах) |
| 11 | машина наклоняется вместо поворота («не самокат»), особенно заметно на Lancia | исправлено 7592d97 |
| 12 | дома посреди трассы на Щёлково и Лиговском | исправлено 49d32ab |
| 13 | хитбоксы машин меньше ограждений (машина залезает в отбойник) | исправлено 7592d97 |
| 14 | машины неповоротливые; Lancia наоборот слишком поворотливая | исправлено 7592d97 (проверить на телефоне) |
| 15 | модель Bugatti испорчена, Ford GT40 перенесён плохо | исправлено 6f4655e (проверить у владельца) |
| 16 | краска в гараже визуально не ложится (точно на Lancia) | исправлено 0a70a39 |
| 17 | финальный босс не похож на Мэдкида, двор у 1703 не похож на себя | исправлено d3155e6 (босс), d5387e4 (двор) |
| 18 | музыка сама переключается после гонок, мешает слушать плейлист | исправлено 4ff2752 |
| 19 | модели зомби и Дедпула заменились на скучные манекены | исправлено 87cd4b4 |

Решения владельца, которые действуют дальше: полёт — пасхалка; Щёлково — день.

## 1. Разбор (FACT / INFERENCE / HYPOTHESIS)

- **#7 FACT** (`src/game/race/RaceScene.ts` `completeLap`, `src/ui/HUD.tsx:46`): `r.lap` — число пройденных кругов,
  HUD пишет «Финиш» при `lap >= total`, а гонка завершается при `lap > laps` — нужен лишний круг. Ошибка с
  первого коммита движка (019f5c5).
- **#6 FACT** (`toggleFly`): при посадке машина ставится на ближайшую точку трассы, но `nextSector` (защита от
  срезок: секторы надо пройти по порядку) остаётся старым — пока машина снова не проедет пропущенный сектор,
  пересечение линии не засчитывается.
- **#2 FACT** (`src/ui/theme.css`): мини-плеер в паузе рендерится (`App.tsx`), но `.wa` z-index 15 под
  `.overlay` z-index 20 с размытием — не виден и не нажимается.
- **#18 HYPOTHESIS** (`src/ui/MusicWidget.tsx`): каждый смонтированный виджет при монтировании вызывает
  `audio.music.play(settings.musicTrack)`; во время гонки виджета нет, автопереход на следующий трек не
  сохраняется в настройки → на экране результатов/в меню новый виджет возвращает старый трек. Проверка: доиграть
  трек во время гонки, выйти в меню — трек не должен смениться.
- **#18 FACT** (`qa/music-check.mjs`, A/B): со старым `MusicWidget` после финиша трек откатился на сохранённый
  («низкий флекс» вместо «ты че обиделась») — `MUSIC_JUMPED`; с 4ff2752 — `MUSIC_OK`. Гипотеза подтверждена.
- **#7/#6 FACT** (`qa/lap-check.mjs`): гонка на 2 круга с полётом на 23–28 с — первый круг 94.5 с засчитан,
  результаты сразу после второго пересечения (180 с).
- **#11/#13/#14 FACT** (код `CarPhysics`): крен 0.022 рад на м/с² (≈30° в повороте на 2.4 g), тангаж 0.014;
  столкновения машин — окружности ≈1.2 м для машин 4–5 м; стена — точка с половиной ширины в 0.6 м от
  отбойника; поперечное сцепление ≈1.2 g. После 7592d97 (`qa/sim.ts`): 1.76 g на ступеньке руля, дрифт 38° с
  выходом на 149 км/ч, ИИ 0–2 касания за 2 круга, 0 разворотов; капсулы: «нос в корму» 5.08 м при длине 4.98.
- **#12 FACT** (проверка рёбер зданий каждые 1 м): Щёлково — стена 15-этажки 60606775 поперёк связки к мосту
  (точка Шмидта привязалась к узлу в 77 м), Лиговский — угол дома 61580024 на повороте Свечной→Марата.
  Причина — выталкивались только вершины. После 49d32ab: вычитание полигона коридора, проверка в конвертере — 0.
- **#1 FACT** (`qa/flicker.mjs`, разность кадров при сдвиге камеры на 2 см): шум внутри горящих окон — хеш
  `fract(sin(x)·43758)` при x ~10⁴ усиливал ошибку интерполяции. Устойчивый хеш + мягкие звёзды:
  3.53 / 3.69 % → 0.84 / 0.57 % (средняя |ΔL| / доля пикселей с изменением > 24/255).
- **#19 FACT** (`/home/n8n/gamers/app`): в старой версии боец — статичная модель Дедпула (31 тыс. треугольников,
  без скелета, `src/assets/deadpool.glb`) с базукой, зомби — Xbot с зелёной кожей и красными глазами.
- **#9 FACT** (`qa/car-look.mjs`, M5 спереди ночью): фары не светятся, ореол `CarGlows` висит над капотом —
  позиции ореолов заданы общими числами, а маска фар в запечённой модели не совпадает с линзами.

- **#17 FACT** (`src/assets/madk1d_face_big.jpg`, 1024², просмотр): «эмблема Мэдкида» на груди меха и в портрете —
  это стандартная заглушка-аватар ВКонтакте (собака в очках), а не логотип артиста; голова меха (собачий череп,
  визор, федора, сигара, золотая цепь) повторяла её. Референс внешности: Commons «Madkid 2026.jpg» (CC BY 4.0) —
  платиновое каре с чёлкой, бледное лицо, пирсинг-септум, чёрный свитер оверсайз, серый шарф.
- **#17 FACT** (`src/game/boss/Arena.ts`): двор 1703 был круглой ареной — розовая брусчатка, кольца разметки,
  полосатые отбойники с неоновой полосой, контейнеры, 12 фонарей по кругу, 3 прожекторных луча, неон на
  крышах. Реальный двор (Commons, Лиговский 50, 2025-03 и Zoccolo 2.0, 2018): асфальтовые проезды между
  двухэтажными складами из тёмно-красного кирпича, настенные прожекторы на кронштейнах, водостоки,
  серебристые спиральные воздуховоды, двери под козырьками-роллетами с афишами, пожарные лестницы,
  припаркованные машины. Свободного места в 58 м там нет (FACT: поиск пустого круга рядом с дверью — не больше
  23 м в радиусе 65 м), поэтому круг боя остаётся, но реквизит арены заменён обвесом двора по фото
  (`src/game/boss/Yard.ts`).
- **#17 FACT** (`qa/boss-look.mjs`, кадры /mnt/ramdisk/spg3d-qa/boss-look/m5-*, m6-*): после d5387e4 двор со всех
  сторон окружён краснокирпичными складами (обрезка контуров кругом вместо удаления), у входа — красная вывеска
  1703, баннер МЭДКИД, козырёк с афишами, прожектор; дальний LOD припаркованных машин вблизи выглядел
  сплющенным — заменён настоящими моделями. `qa/boss-shot.mjs`: 81–86 draw calls (low), 89–94 (medium), ошибок нет.
- **#15 FACT** (`/mnt/ramdisk/spg3d-qa/garage-gt40-v2.png`, после 6f4655e): GT40 цел — кузов, полосы на порогах,
  колпаки фар, колёса Goodyear; открытых дефектов не найдено, нужна проверка владельцем.
- **#4 FACT** (`qa/track-tour.mjs shchyolkovo medium`, кадры /mnt/ramdisk/spg3d-qa/day/t1-*): после простого переключения
  на день город выглядел пусто — плоская серая дорога и тротуары, высокие серые отбойники, почти нет деревьев, нет
  теней (тени были только на high), пустое небо. После 0cf4698 (t4-*, v4-*): облака, тени от домов и деревьев с
  medium, газоны, аллеи лип вдоль тротуаров и деревья/кусты на газонах, чёрная ограда на гранитном цоколе,
  плитка, бордюры, скамейки и урны; 43–116 draw calls на medium. Треугольники на medium доходили до 346 тыс. —
  лимит деревьев на medium снижен до 650.
- **#8 FACT** (код `SkidMarks`): следы были отдельными чёрными квадами по сегменту (клинья и двойная прозрачность
  на поворотах), без профиля и затухания, только у игрока; оранжевые «пятна света» — аддитивные декали 11×11 м
  под каждым фонарём поверх освещения в шейдере дороги. **#10 FACT**: в шейдере дороги лужи были только зеркальными
  пятнами без анимации.
- **#4 FACT** (`qa/view.mjs`, /mnt/ramdisk/spg3d-qa/day/lm-0, pr2-*): в OSM у отеля «Премиум» есть только круг короны
  19 м, поэтому ориентир был гладким красным цилиндром; по фото с Commons он собран заново (подиум, кирпичная
  плита с мелкими окнами, синий эркер, серый верх, корона с кольцом). Река получила рябь, у отеля — два фонтана;
  зебры на трассе у переходов и светофоров. Треугольники на medium по туру 228–291 тыс.
- **#5 FACT** (кадры /mnt/ramdisk/spg3d-qa/day/lig4-0.200 → night6-*): ночью дорога была чёрной, под машиной игрока
  жёлтый эллипс подсветки, на отбойниках голубые/жёлтые неоновые полосы, декаль фар с прозрачностью 16 % не
  видна. После 1aa5a17: конус ближнего света на асфальте, пятна под фонарями, ограды вместо неона.
- **QA FACT** (`qa/shots.mjs`, suite5, сборка a4dc0b0): 6 pass / 3 fail; все три падения — «geometries stable»
  на экране результатов (однокруговая гонка кончилась до 45 с после исправления #7), бюджеты на 20 с — 62–107
  draw calls, 128–181 тыс. треугольников; харнесс исправлен d1b51db.

## 2. Выпуск 2.4.0 (2026-09-15)

- **FACT** `qa/shots.mjs` suite6 на сборке 2.4.0: 9 pass / 0 fail; гонки (low) 47–72 draw calls, 149–158 тыс.
  треугольников; босс 87 (desktop) / 91 (mobile) draw calls; утечек нет (geometries 47 → 47 после двух гонок).
- **FACT** подписанный APK `release/spg3d-2.4.0-release.apk` — 40.5 МБ, versionCode 6 / 2.4.0, sha256
  `2576239b…8675dc5`, `apksigner verify` — CN=SPG3D.
- **FACT** GitHub Pages обновлены из той же сборки (gh-pages c65b5eb); страница отдаёт `assets/index-PZZrRKYJ.js`,
  как локальный dist.
- Открыто: #14 — проверка управления на телефоне владельцем; #15 — оценка GT40/Bolide владельцем; дальнейшая
  детализация Щёлково (пешеходы, фасады) — по отзыву.
